[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/foundit-backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/v1/auth/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
